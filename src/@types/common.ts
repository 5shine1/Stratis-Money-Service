export interface IIcon {
  className: string;
}

export interface ICurrency {
  img?: string;
  id: string;
}

export interface IAuth {
  accessToken: string;
  refreshToken: string;
  email: string;
}
